<div class="h-full w-full flex flex-col gap-10 bg-stone-800 sm:px-20 px-8 py-8">
  <% if current_user.nil? %>
    Please sign in.
  <% else %>
    <%= render Deployments::GridComponent.new(deployments: current_user.team.deployments) %>
  <% end %>
</div>
