<div class="h-full w-full flex flex-col gap-10 bg-stone-800 sm:px-20 px-8 py-8">
  <div class="flex w-full justify-between">
    <div class="flex flex-col">
      <span class="text-xs sm:text-sm text-stone-400 font-medium text-stone-100">Service</span>
      <span class="sm:text-2xl font-medium text-stone-100 w-fit"><%= @service.name %></span>
    </div>

    <div class="flex flex-col justify-center">
      <div class="flex gap-2 items-center">
        <%= link_to edit_project_version_service_path, class: 'p-2 rounded-md bg-stone-800 hover:bg-stone-700' do %>
          <%= heroicon 'cog-8-tooth', variant: :outline, class: 'size-5' %>
        <% end %>
      </div>
    </div>
  </div>

  some stuff for the service
</div>
