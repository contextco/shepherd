<li class="grid items-stretch">
  <%= link_to version_path(project.latest_project_version), class: 'w-full border border-solid border-stone-400 p-4 text-primary cursor-pointer duration-200 outline-2' do %>
    <h6 class="text-primary type-body-02-semi-bold mb-6 [container-type:inline-size]">
      <span class="block truncate w-[100cqi] font-medium"><%= project.name %></span>
    </h6>
    <div class="flex items-center gap-2 h-7">
    </div>
  <% end %>
</li>
