<%= form_with url: project_index_path, class: 'flex flex-col gap-6', method: :post do |form| %>
  <%= render Applications::FieldsForApplicationFormComponent.new(form:) %>

  <%= render Applications::FieldsForApplicationVersionFormComponent.new(form:) %>

  <%= form.submit "Create", class: 'px-3 py-2 mt-2 bg-stone-700 rounded-md text-stone-100 cursor-pointer w-fit' %>
<% end %>
