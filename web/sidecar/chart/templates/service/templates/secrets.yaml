apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-secrets
  labels:
    app: {{ .Release.Name }}
type: Opaque
data:
  {{- range .Values.secrets }}
  {{ .environmentKey }}: {{ .value | b64enc }}
  {{- end }}
