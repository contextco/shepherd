apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
type: Opaque
data:
  {{- range .Values.secrets }}
  {{ .environmentKey }}: {{ .value | b64enc }}
  {{- end }}
